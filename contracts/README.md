# Contracts

This directory is reserved for Soroban smart contracts.

The Rust workspace root is defined in the repository `Cargo.toml`, and contract
crates should live under `contracts/`.

The current placeholder crate exists only to keep the workspace and CI checks
valid while the real on-chain implementation is still being scaffolded.
