# Services

This directory contains off-chain services and shared service-side packages.

Current placeholders:

- `api/` for public read APIs and explorer-facing endpoints
- `bridge/` for listener and relayer-oriented services
