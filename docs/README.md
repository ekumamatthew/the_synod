# Documentation

This directory contains tracked project documentation intended to live with the
repository.

Key areas:

- `adr/` for architecture decision records
- `development/` for local setup and configuration guidance
- `maintainers/` for maintainer-facing workflow guidance
